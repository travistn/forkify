'use client';

import RecipeBookHeader from './RecipeBookHeader';

export default function RecipeBook() {
  return (
    <section className='w-full min-h-screen bg-pale-orange xl:rounded-md xl:w-[80%] xl:mx-auto 2xl:w-[64%]'>
      <RecipeBookHeader />
    </section>
  );
}
